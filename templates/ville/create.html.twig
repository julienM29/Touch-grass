{% extends 'layout/base.html.twig' %}

{% block title %}Insert new Ville{% endblock %}

{% block main %}
    <h1>Créer une nouvelle ville</h1>

    <section>
        {{ form_start(villeForm) }}
        {{ form_widget(villeForm) }}
    </section>

    <button type="submit">Enregistrer</button>
    {{ form_end(villeForm) }}

{% endblock %}
